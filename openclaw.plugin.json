{
	"id": "openclaw",
	"kind": "memory",
	"activation": {
		"onStartup": true
	},
	"contracts": {
		"tools": [
			"hydradb_query",
			"hydradb_ingest",
			"hydradb_list",
			"hydradb_inspect",
			"hydradb_delete",
			"hydra_search",
			"hydra_store",
			"hydra_list_memories",
			"hydra_get_content",
			"hydra_delete_memory"
		]
	},
	"uiHints": {
		"apiKey": {
			"label": "Hydra API Key",
			"sensitive": true,
			"placeholder": "your-hydra-api-key",
			"help": "Your API key from Hydra (or use ${HYDRA_OPENCLAW_API_KEY})"
		},
		"tenantId": {
			"label": "Tenant ID",
			"placeholder": "tenant-01",
			"help": "Your Hydra tenant identifier (or use ${HYDRA_OPENCLAW_TENANT_ID})"
		},
		"subTenantId": {
			"label": "Sub-Tenant ID",
			"placeholder": "hydra-openclaw-plugin",
			"help": "Sub-tenant for data partitioning (default: hydra-openclaw-plugin)",
			"advanced": true
		},
		"autoRecall": {
			"label": "Auto-Recall",
			"help": "Inject relevant memories before every AI turn (default: true)"
		},
		"autoCapture": {
			"label": "Auto-Capture",
			"help": "Automatically store conversation exchanges after every AI turn (default: true)"
		},
		"maxRecallResults": {
			"label": "Max Recall Results",
			"placeholder": "10",
			"help": "Maximum memory chunks injected into context per turn",
			"advanced": true
		},
		"recallMode": {
			"label": "Recall Mode",
			"help": "'fast' (default) or 'thinking' (personalised recall with deeper graph traversal)",
			"advanced": true
		},
		"graphContext": {
			"label": "Graph Context",
			"help": "Include knowledge graph relations in recalled context (default: true)",
			"advanced": true
		},
		"ignoreTerm": {
			"label": "Ignore Term",
			"placeholder": "hydra-ignore",
			"help": "Messages containing this term will be excluded from recall and capture (default: hydra-ignore)"
		},
		"layout": {
			"label": "Database Layout",
			"help": "'auto' (default) reads it from Hydra; 'unified' for a database created with type=unified (one corpus, no kind); 'split' for the classic knowledge/memory layout",
			"advanced": true
		},
		"debug": {
			"label": "Debug Logging",
			"help": "Enable verbose debug logs for API calls and responses",
			"advanced": true
		}
	},
	"configSchema": {
		"type": "object",
		"additionalProperties": false,
		"properties": {
			"apiKey": { "type": "string" },
			"tenantId": { "type": "string" },
			"subTenantId": { "type": "string" },
			"autoRecall": { "type": "boolean" },
			"layout": { "type": "string", "enum": ["auto", "split", "unified"] },
			"autoCapture": { "type": "boolean" },
			"maxRecallResults": { "type": "number", "minimum": 1, "maximum": 50 },
			"recallMode": { "type": "string", "enum": ["fast", "thinking"] },
			"graphContext": { "type": "boolean" },
			"ignoreTerm": { "type": "string" },
			"debug": { "type": "boolean" }
		},
		"required": []
	}
}
